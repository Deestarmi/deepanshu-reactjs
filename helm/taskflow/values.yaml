# ──────────────────────────────────────────────
#  TaskFlow Helm Values
#  Edit YOUR_DOCKERHUB_USERNAME before deploying
# ──────────────────────────────────────────────

frontend:
  image:
    repository: deestar08/taskflow-frontend
    tag: "8038119d78e382ab117f6dc20dd7ef75db2a5091"
    pullPolicy: IfNotPresent
  replicaCount: 1
  service:
    type: LoadBalancer   # Gets a public IP from Azure
    port: 80
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

backend:
  image:
    repository: deestar08/taskflow-backend
    tag: "8038119d78e382ab117f6dc20dd7ef75db2a5091"
    pullPolicy: IfNotPresent
  replicaCount: 1
  service:
    type: ClusterIP      # Internal only — frontend proxies to it
    port: 3001
  env:
    APP_VERSION: "1.0.0"
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi
